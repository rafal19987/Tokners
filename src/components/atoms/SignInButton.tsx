const SignInButton = () => {
  return (
    <button className="w-fit text-white font-bold hover:text-black hover:cursor-pointer transition-colors duration-200">
      Sign In
    </button>
  );
};

export default SignInButton;
